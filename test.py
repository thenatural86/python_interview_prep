def version_compare( version1, version2 ) :
	#Insert your code here
    if version1 == version2:
        return 0
    if version1 > version2:
        return 1
    if version1 < version2:
        return -1
    float(version2)
