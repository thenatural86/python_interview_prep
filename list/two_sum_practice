def two_sum(numbers,target):
    num_map = {}
    
    for i,num in enumerate(numbers):
        diff = target - num
        if diff in num_map:
            return [num_map[diff], i]
        num_map[num] = i
            